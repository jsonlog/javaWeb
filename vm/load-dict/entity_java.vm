package ${app_package};

import smart.framework.base.BaseEntity;

public class ${entity_name_p} extends BaseEntity {
#foreach($field in ${field_list})
#if($field.name != "id")

    private ${field.type} ${field.name}; // ${field.comment}
#end
#end
#foreach($field in ${field_list})
#set($fieldName = $SU.firstToUpper(${field.name}))
#if($field.name != "id")

    public ${field.type} get${fieldName}() {
        return ${field.name};
    }

    public void set${fieldName}(${field.type} ${field.name}) {
        this.${field.name} = ${field.name};
    }
#end
#end
}
